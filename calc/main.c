#include <stdio.h>
#include <stdlib.h>
#include "calc.h"

int main()
{
int r;
r=add(4,5);
printf("the sum is %d\n",r);
r=sub(55,5);
printf("the sub is %d\n",r);
r=divi(40,5);
printf("the divi is %d\n",r);
r=mul(4,5);
printf("the mul is %d\n",r);
}
